{
  "name": "iframe-preview-app",
  "version": "1.0.0",
  "description": "A TypeScript-based iframe preview application with modern UI",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^5.3.3"
  }
}
